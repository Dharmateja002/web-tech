# Q8 - zips.json Import + Aggregation Pipeline Answers

This folder contains:
- `commands.md`: import command + aggregation queries for all 3 sub-questions.

Dataset URL in question: `http://media.mongodb.org/zips.json`

Open `commands.md` and run each command in order.

